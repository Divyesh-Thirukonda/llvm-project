import sys

# GPU tests may require GPU hardware or emulation
# These tests demonstrate the transformation pipeline but may not execute
# without appropriate GPU runtime support

# No JIT on win32.
if sys.platform == "win32":
    config.unsupported = True
